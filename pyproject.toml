[project]
name = "test"
version = "0.1.0" add local
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [] add in local
dependencies = [] add in cloud
